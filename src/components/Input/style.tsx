import styled from 'styled-components';

const Container = styled.input`
  font-family: inherit;
  font-size: 1.4rem;
  line-height: 2rem;
  outline: none;
  border: none;
`;

export default Container;
