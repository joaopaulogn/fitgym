import React from 'react';
// import { isValidEmail, isValidPassword } from '../../Form/Input/validator';

function validateSignIn(event: React.FormEvent): void {
  event.preventDefault();
}

export default validateSignIn;
